import React, { PropTypes } from 'react';

class AboutPage extends React.Component {
  render () {
    return (
      <p> About page needs authentification</p>
    );
  }
}

export default AboutPage;
